# Docker

## Run the application

The frontend, backend, and Redis can be started using Docker Compose:

```powershell
docker compose up --build
```

Once running, the application can be accessed at:
http://127.0.0.1:8080

To stop and remove the containers:

```powershell
docker compose down
```